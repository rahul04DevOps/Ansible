---
- name: using multiple conditions
  hosts: managednode1
  tasks:
  - package: 
       name: httpd
       state: installed
    when:
      - ansible_distribution == "RedHat"
#      - ansible_memfree_mb > 1512
